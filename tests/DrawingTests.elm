module DrawingTests exposing (suite)

import Drawing exposing (ArrowAttributes, Drawing(..))

suite : Test
suite =
  describe "Drawing module"
      [ describe "calcLinePos"
          [ 
          ]
      ]